# 电商促销评估系统 — 项目说明文档

---

## 一、行业背景

### 1.1 促销决策的困境

电商促销已成为平台获取增量订单的核心手段，"双11""618"等大促活动创造了数以千亿计的成交额。然而，粗放式的全量促销策略正面临日益严峻的挑战：

- **ROI持续下滑**：用户对优惠券、满减的敏感度逐年降低，促销成本越来越高，但边际转化收益越来越小。
- **"薅羊毛"用户问题**：大量用户本身会自然购买，即便没有促销激励也会下单，对这类用户发放优惠券只是纯粹的利润损耗，不带来增量。
- **抗拒型用户被打扰**：部分用户对频繁的促销推送产生逆反心理，反而降低了购买意愿，形成负向效果。
- **预算分配缺乏科学依据**：业务团队往往依赖历史经验或A/B测试直觉来决定投放方案，缺乏系统性的因果分析支撑。

### 1.2 精准营销的核心命题

真正有价值的促销投放，核心问题不是"哪些用户可能购买"，而是"哪些用户**因为促销**才会购买"。这两者之间的差异，正是**增量价值（Uplift）**的本质所在。

传统机器学习模型擅长预测转化概率，但无法区分"促销驱动的转化"与"自然发生的转化"。因果推断方法的引入，使得从数据中识别真正受促销影响的用户群体成为可能。

### 1.3 数据背景

本项目融合了两类真实数据集：

| 数据集 | 来源 | 规模 | 用途 |
|---|---|---|---|
| WA Marketing Campaign | IBM Watson Analytics | 548条门店级促销记录 | 促销方案效果评估 |
| UserBehavior（阿里巴巴） | 天池公开数据集 | 百万级用户行为日志 | 用户转化建模与分群 |

---

## 二、功能介绍

系统共包含 **10 个功能模块**，按业务场景分为三大板块。

---

### 板块一：智能预测

#### 🔮 转化预测（实时单用户）
输入用户的行为特征（浏览次数、收藏次数、活跃天数、峰值小时、周末占比、偏好品类），实时返回该用户的**转化概率**、转化分层（高/中/低）和营销建议。适用于运营人员对特定用户做快速判断的场景。

#### 📤 批量分析（CSV批量四象限分类）
支持上传包含多用户特征的 CSV 文件，系统批量运行 XGBoost 模型，输出每位用户的：
- 转化概率（0~1）
- 分层标签（A-高转化 / B-中转化 / C-低转化）
- 运营建议（是否投放促销资源）

结果以**分群占比饼图** + **可下载明细表**呈现，可直接导出供投放系统使用。

#### 🔍 用户查询（单用户画像）
按用户ID精确查询该用户的完整画像，包括 RFM 三维指标、偏好品类、活跃规律、转化概率及价值分层，适合客服和精细化运营场景。

---

### 板块二：促销效果分析

#### 🎯 KPI总览
汇总展示四个核心指标：整体转化率、总 GMV（万元）、PSM 因果增量（K$/店/周）、平均 Uplift 提升幅度（%）。提供各促销方案的均值对比，让管理层一眼掌握整体促销健康度。

#### 📉 转化漏斗
呈现用户从**全量 → 浏览 → 收藏 → 购买**的四级漏斗，量化每级的流失率，并展示购买用户在各品类上的分布，辅助优化落地页和商品选品。

#### 📊 销售趋势
展示三种促销方案（满减/折扣/优惠券）在四周内的销售额走势，支持单方案筛选查看，直观对比活动前后的销售节奏差异。

#### ⚖️ ATE因果效应对比
这是本系统的核心分析模块，集成了三种因果推断方法，从不同角度量化促销的真实增量效果：

| 方法 | 原理 | 核心结论 |
|---|---|---|
| **PSM 倾向得分匹配** | 对门店规模、年龄等混淆变量进行匹配，构造可比对照组 | 方案一对方案二有显著正向因果增量（p=0.033） |
| **DiD 双重差分** | 比较活动前后两组销售额的差中差 | 方向与PSM一致，但因观测期短未达显著性（p=0.48） |
| **Uplift T-Learner** | 分别拟合处理组和控制组的响应模型，输出个体因果效应（CATE） | 促销平均提升用户转化概率约 +X%（p<0.001） |

---

### 板块三：用户行为与分群分析

#### 🧩 Uplift 四象限用户分群
基于 T-Learner 计算出每位用户的 CATE（个体因果效应），将用户划分为四类：

| 分群 | 特征 | 运营策略 |
|---|---|---|
| **可说服型（优先触达）** | CATE 高、转化概率中等 | 优先投放促销资源，ROI 最高 |
| **自然转化型（无需促销）** | 无论是否促销都会购买 | 避免过度触达，节省资源 |
| **待激活型（潜力用户）** | 当前响应极低，有潜力 | 需更强个性化激励才能激活 |
| **抗拒/自然型** | 促销反而降低购买意愿 | 避免打扰，以内容运营为主 |

#### 💎 RFM 用户价值三维分布
以 R（最近一次购买距今天数）× F（购买频次）× M（消费金额）三维气泡图可视化用户价值分布，支持按高/中/低价值分群筛选，同时展示各分群的均值对比及偏好品类分布。

---

## 三、项目实现

### 3.1 技术架构

```
┌─────────────────────────────────────────┐
│              浏览器（前端）               │
│  HTML5 + CSS3 + Vanilla JS + ECharts    │
└──────────────────┬──────────────────────┘
                   │ HTTP / JSON API
┌──────────────────▼──────────────────────┐
│           Flask 后端（Python）           │
│  RESTful API + Session 认证 + 权限装饰器  │
└──────────────────┬──────────────────────┘
                   │
       ┌───────────┼───────────┐
       ▼           ▼           ▼
   CSV 数据      XGBoost     scikit-learn
  (Pandas)       模型       (PSM / RFM)
```

- **前端**：纯原生实现，无框架依赖，ECharts 5 负责所有图表渲染
- **后端**：Flask + Flask-CORS，提供 REST API，Session 管理登录状态
- **数据层**：Pandas 在内存中预加载全量数据，避免每次请求重复 IO
- **模型层**：XGBoost（joblib 序列化），scikit-learn 的 NearestNeighbors 用于 PSM 匹配

---

### 3.2 核心算法实现

#### 3.2.1 XGBoost 转化预测模型

**训练特征（6维）：**

| 特征 | 含义 |
|---|---|
| `cnt_pv` | 浏览次数 |
| `cnt_fav` | 收藏次数 |
| `fav_category_code` | 偏好品类编码 |
| `active_days` | 活跃天数 |
| `peak_hour` | 最常活跃时段 |
| `weekend_pct` | 周末行为占比 |

> 注意：训练时刻意**排除购买行为特征**，避免标签泄漏。模型学习的是"行为过程"而非"购买结果"。

**概率校准：** 为解决 XGBoost 离散输出问题，对原始概率进行特征感知平滑，利用指数函数将浏览/收藏数映射为连续概率变化，输出范围约束在 [0.05, 0.97]。

**模型表现（UserBehavior 数据集）：**
- AUC = 0.96 | KS = 0.79 | Top 10% Lift = 8.8

---

#### 3.2.2 PSM 倾向得分匹配（ATE估计）

```
1. 用逻辑回归拟合倾向得分 P(T=1 | X)
   — 协变量 X：门店规模（MarketSize）、市场年龄（AgeOfStore）
2. 使用最近邻匹配（k=1）为每个处理组样本找对照
3. 计算匹配后样本的平均处理效应 ATT
   ATT = mean(Y_treated) - mean(Y_matched_control)
```

匹配使用 scikit-learn `NearestNeighbors`，特征经 `StandardScaler` 标准化后计算欧氏距离。

---

#### 3.2.3 DiD 双重差分

```
DiD = (post_treated - pre_treated) - (post_control - pre_control)
     = 处理组活动前后之差 - 对照组活动前后之差
```

以第1、2周为"活动前"，第3、4周为"活动中"，方案一为处理组，方案二为对照组。

---

#### 3.2.4 T-Learner Uplift 建模

```
1. 训练两个独立模型：
   μ₁(x) = E[Y | T=1, X=x]  — 促销组响应模型
   μ₀(x) = E[Y | T=0, X=x]  — 对照组响应模型

2. 计算个体因果效应（CATE）：
   τ(x) = μ₁(x) - μ₀(x)

3. 按 CATE 与转化概率二维划分四象限
```

---

### 3.3 权限体系

系统实现了三级角色权限管控：

| 角色 | 可访问模块 |
|---|---|
| **admin（管理员）** | 全部功能 + 账号管理 |
| **analyst（分析师）** | 全部分析功能（含批量上传），无账号管理 |
| **viewer（观察者）** | 仅 KPI 总览 + 销售趋势 |

**实现机制：**
- 后端：`@role_required('权限点')` 装饰器，所有 API 接口均有保护
- 前端：`data-perm` 属性 + `hasPerm()` 函数控制卡片显示/隐藏
- 持久化：用户信息存储于 `users.json`（SHA-256 密码哈希）

---

### 3.4 数据流转

```
CSV 原始数据
    ↓ Pandas 预处理（特征工程）
user_features.csv ──→ RFM计算、行为特征提取
    ↓
XGBoost 模型训练（离线）
    ↓
user_convert_scores.csv  ← 批量推理结果
log_uplift_result.csv    ← T-Learner CATE 结果
    ↓
Flask API 加载至内存
    ↓
前端实时查询 / 批量上传 → JSON响应 → ECharts渲染
```

---

### 3.5 项目文件结构

```
电商/
├── backend/
│   ├── app.py                  # Flask 主程序（全部 API + 权限逻辑）
│   ├── users.json              # 用户数据库（JSON持久化）
│   ├── data/
│   │   ├── WA_Marketing-Campaign.csv     # 促销实验数据
│   │   ├── user_features.csv             # 用户特征（RFM+行为）
│   │   ├── user_convert_scores.csv       # 模型推理分数
│   │   ├── feature_importance.csv        # 特征重要性
│   │   ├── log_uplift_result.csv         # Uplift CATE 结果
│   │   └── xgb_ub_model.pkl              # 训练好的XGBoost模型
│   └── static/
│       ├── index.html           # 首页（功能导航）
│       ├── login.html           # 登录页
│       ├── user_predict.html    # 转化预测
│       ├── user_batch.html      # 批量分析
│       ├── user_profile.html    # 用户查询
│       ├── user_segments.html   # 四象限用户分群
│       ├── user_rfm.html        # RFM三维分布
│       ├── promo_overview.html  # KPI总览
│       ├── promo_funnel.html    # 转化漏斗
│       ├── promo_trend.html     # 销售趋势
│       └── promo_ate.html       # ATE因果效应对比
└── 数据处理/                    # 数据预处理脚本（Jupyter Notebook）
```

---

### 3.6 启动方式

```bash
# 安装依赖
pip install flask flask-cors pandas numpy scipy scikit-learn xgboost joblib

# 启动服务
cd backend
python app.py

# 访问地址
http://localhost:5000
```

---

*文档版本：v1.0 | 2026-03-29*
